---
driver:
  name: docker
platforms:
  - name: molecule-instance
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - /var/run/docker.sock:/var/run/docker.sock:rw
    privileged: true
    dockerfile: ../_shared/Dockerfile.j2
    build_image: true
    pre_build_image: false
    keep_volumes: false
provisioner:
  name: ansible
  playbooks:
    prepare: prepare.yml
    converge: converge.yml
    cleanup: cleanup.yml
    side_effect: ../_shared/cleanup-misc.yml
    verify: verify.yml
role_name_check: 1
scenario:
  name: demo-extcfg
  test_sequence:
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - side_effect
    - destroy
...
