---
- name: Verify Netbird Stack
  hosts: all
  gather_facts: False
  vars:
    compose_stack_name: "netbird-dev"
  tasks:
    - name: Include shared verification tasks
      ansible.builtin.include_tasks: ../_shared/verify-common.yml
...
