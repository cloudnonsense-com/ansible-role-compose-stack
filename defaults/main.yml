---
# defaults file for ansible-role-compose-stack
app: hello_world
apps_dst_dir: "/opt/apps"
app_dst_dir: "{{ apps_dst_dir }}/{{ app }}"

compose_src_file: "{{ 'files/compose.yml' if app == 'hello_world' else 'apps/' ~ app ~ '/compose.yml' }}"
