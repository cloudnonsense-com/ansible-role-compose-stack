services:
  nginx-demo:
    container_name: nginx-demo
    image: ubuntu/nginx:latest
    ports:
      - "37080:80"
    restart: unless-stopped
    volumes:
      - "{{ dst_app_data }}/index.html:/usr/share/nginx/html/index.html:ro"
