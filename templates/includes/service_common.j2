{# includes/service_common.j2 #}
    container_name: "{{ svc.container_name | default(stack.name) }}"
    image: "{{ svc.image }}"
    restart: "{{ svc.restart | default('unless-stopped') }}"
{# end #}