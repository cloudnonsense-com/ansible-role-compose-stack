---

- ansible.builtin.include_vars:
    file: "{{ role_path }}/defaults/{{ compose_stack_name }}.yml"

- include_tasks: create.yml
  when: compose_stack_state == "present"

- include_tasks: destroy.yml
  when: compose_stack_state == "absent"

...